/** Tunables for snap-tower V1 (portfolio build path). */
export const PARTS = {
  maxSticks: 12,
  maxMarshmallows: 10,
}

export const STABILITY = {
  minOccupiedNodes: 2,
  minEdgesForTest: 1,
  minBaseSpread: 0.18,
  topHeavyWarn: 1.35,
}

export const SIZES = {
  jointRadius: 0.055,
  stickRadius: 0.012,
  topRadius: 0.06,
}
